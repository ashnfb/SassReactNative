# NFB REACT-NATIVE SASS
# SETUP

```
yarn install
cd ios
pod install
cd ..
brew install sass/sass/sass
npx react-native-start
```

choose i to launch the iOS emulator


Example sass sheet is in styles/App.scss